---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yml
  - service.yml

configMapGenerator:
  - name: prometheus-cm
    files:
      - data/prometheus.yml
